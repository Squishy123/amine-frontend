extends layout

block content
  div(class="container justify-content-center" style="margin-top: 15px")
    div(class="jumbotron")
      h1(class="display-4")= title
      p(class="lead") Welcome to #{title}
